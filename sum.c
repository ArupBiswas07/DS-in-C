#include <stdio.h>

int main() {
    int a, b, sum;

    // Dueta number user pora lobo
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Jog koribo
    sum = a + b;
     
    // Result print koribo
    printf("Sum = %d\n", sum);

    return 0;
}